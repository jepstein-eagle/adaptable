{
  "name": "adaptableblotter-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.1.4",
    "copyfiles": "^2.1.0"
  },
  "scripts": {
    "cleanup": "lerna clean --yes",
    "publish": "lerna publish"
  }
}
