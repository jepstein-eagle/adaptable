/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdaptableblotterHyperGridComponent } from './adaptableblotter-hypergrid.component';

describe('AdaptableblotterHyperGridComponent', () => {
  let component: AdaptableblotterHyperGridComponent;
  let fixture: ComponentFixture<AdaptableblotterHyperGridComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdaptableblotterHyperGridComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdaptableblotterHyperGridComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

*/